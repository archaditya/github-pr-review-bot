# apps/web/.env — copy from this template

# Base URL of apps/api. The dashboard talks to it directly over REST (never to Postgres,
# GitHub, or ai-service — see apps/web/README.md).
NEXT_PUBLIC_API_URL=http://localhost:4000
